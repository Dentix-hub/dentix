# Development, CI, and test-only dependencies. Install this beside either the
# cross-platform requirements.txt or the Linux production requirements.lock.

pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0
bandit>=1.7.0
pip-audit>=2.7.0
