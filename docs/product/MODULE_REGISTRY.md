# Dentix Module Registry

Status notes describe inventory confidence, not feature quality. `PARTIAL` means the module exists but some routes/capabilities/ownership details were not exhaustively proven in Plan 01.

| Module | Frontend owner path | Backend owner path | UI routes / entry points | API namespace / router | Roles / permissions (verified boundary) | Important models / data | Main screens/actions | Important tests / evidence | Status notes |
|---|---|---|---|---|---|---|---|---|---|
| Dashboard | `frontend/src/pages/Dashboard*` | `backend/routers/dashboard.py`, dashboard services | `/` after auth | dashboard router under API v1 | Authenticated UI; API permissions are endpoint-owned | Aggregated clinic data | Operational overview | Dashboard frontend tests; router/service code | VERIFIED existence; metric semantics remain endpoint/service-owned |
| Patients | `frontend/src/pages/Patients*`, `frontend/src/features/patients/` | `backend/routers/patients.py`, patient/search/visibility services | `/patients`, `/patients/:id` | `/api/v1/patients` | `PATIENT_CREATE/READ/UPDATE/DELETE/SEARCH`; doctor visibility rules also apply | Patient and related clinical/attachment data | Directory/search, create/update, patient details | patient tests + visibility service + router | VERIFIED core module |
| Appointments | `frontend/src/pages/Appointments*` and appointment components/API | `backend/routers/appointments.py`, appointment service/CRUD | `/appointments` | `/api/v1/appointments` | granular appointment permissions; doctor-scoped behavior is enforced server-side where applicable | Appointment + patient/doctor relationships | Schedule/list/update/cancel flows | appointment tests/e2e + router | VERIFIED core module |
| Dental / Clinical | patient-details/dental feature paths including `frontend/src/features/dental/` | treatments, procedures, prescriptions, medications routers/services | Primarily patient detail workflow; print prescription at `/print/rx/:id` | treatment/procedure/prescription/medication namespaces under API v1 | `CLINICAL_READ`, `CLINICAL_WRITE`, `TREATMENT_PLAN_WRITE` depending on endpoint | Treatment, treatment session, procedure, prescription, medication | Dental chart, treatment/clinical actions, prescription output | treatment/service/RBAC tests + route registration | PARTIAL: no single top-level clinical route; module spans patient workflow |
| Finance / Billing / Expenses | `frontend/src/features/finance/`, legacy billing redirect, expenses page | payments, expenses, accounting, financials, metrics routers/services | `/finance/*`; `/billing*` redirects to `/finance/overview`; legacy `/expenses` | multiple API v1 namespaces including payments/expenses/accounting/financials/metrics | Finance route allows admin/super_admin/manager/accountant/receptionist/doctor; server `FINANCIAL_READ/WRITE` remains authoritative | Payments, expenses, salary/lab payments and related financial data | overview, patient accounts, payments, expenses, compensation, payroll, activity, reports | `backend/tests/test_financial_truth.py`, finance tests, E2E billing/expenses | VERIFIED module; metric formulas remain contract/service-owned |
| Analytics | `frontend/src/features/analytics/` | analytics/metrics routers and services | `/analytics` | analytics/metrics namespaces under API v1 | UI restricted to admin/super_admin; server endpoint rules are authoritative | Aggregated analytics data | Smart dashboard/analytics views | analytics route/API evidence | PARTIAL: not every analytic metric inventoried |
| Labs | `frontend/src/pages/Labs*`, lab feature/API | `backend/routers/laboratories.py` and lab services/CRUD | `/labs` | laboratories router under API v1 | UI restricted to admin/super_admin; server endpoint rules authoritative | Laboratory, lab order/payment data | Lab order lifecycle and finance-related actions | lab E2E/tests + router | VERIFIED existence; exhaustive lifecycle not re-derived here |
| Inventory | `frontend/src/pages/Inventory*`, `frontend/src/features/inventory/` | inventory, inventory_smart, financials-related costing services | `/inventory` | inventory and inventory-smart API v1 routers | `INVENTORY_READ`, `INVENTORY_MANAGE` at server boundaries where required | warehouses, materials, batches, stock items, usage/learning records | stock/material/batch and smart inventory workflows | inventory tests/e2e + RLS migration evidence | VERIFIED existence; smart-learning semantics PARTIAL |
| Users | `frontend/src/pages/UsersManager*` | `backend/routers/users.py`, admin doctor/user services | `/users` | users/admin doctor namespaces | UI admin/super_admin; server RBAC authoritative | User, role/clinic relationships | user/team management | RBAC/user tests + route guard | VERIFIED existence |
| Settings | `frontend/src/pages/Settings*`, `frontend/src/features/settings/` | settings, price_lists, insurance and related admin routers | `/settings`, `/settings/price-lists`, `/settings/insurance` | settings/price-list/insurance API v1 namespaces | UI admin/super_admin; system config and endpoint rules server-side | System/tenant settings, price lists, insurance providers | clinic/system settings, pricing and insurance setup | settings E2E/API/router evidence | VERIFIED existence |
| AI | `frontend/src/features/ai/`, AI-related admin page | `backend/ai/`, `backend/services/ai/`, `backend/routers/ai*.py`, analytics AI router | embedded assistant surfaces; `/ai/stats` for super_admin | AI/AI-admin/AI-assist/analytics-AI API v1 routers | `AI_CHAT`; unsafe execution permission where required; stats UI super_admin | AI logs/config/tool outputs | assistant interactions and admin stats | AI governance/security/tool tests | VERIFIED stack existence; active provider/model is dynamic config, not copied here |
| Super Admin | `frontend/src/pages/admin/`, `frontend/src/features/admin/` | admin tenants/subscriptions/system/audit/stats/security/features/doctors + compatibility system_admin | `/admin` and `/admin/*`; `/ai/stats` | admin/system namespaces under API v1 | UI `super_admin`; backend super-admin/RBAC dependencies | tenants, subscriptions, audit/security/system data | overview, tenants, users, finance, communications, system settings/logs | admin/RBAC/tenant-isolation tests + route guards | VERIFIED existence |
| Auth / Public / PWA | `frontend/src/auth/`, login/register/reset/terms/privacy pages, `InstallPrompt`, `vite.config.js` | `backend/routers/auth/`, password reset, health/public settings routes | unauth: `/`, `/login`, `/register`, `/forgot-password`, `/reset-password`, `/terms`, `/privacy`; authenticated public-support/profile routes also exist | `/api/v1/auth/*`, health/global settings and related endpoints | Public entry routes plus authenticated session boundary; CSRF applies to cookie-auth state changes as implemented | User/session/reset data | login/register/reset, session bootstrap, installable PWA | auth tests/e2e + `backend/main.py` + VitePWA config | VERIFIED web/PWA; API requests are NetworkOnly in PWA runtime caching |
| Mobile | `dentix_mobile/` | shared Dentix backend API | Flutter application tree; platform targets include Android/iOS/web/desktop scaffolding | consumes backend APIs (exact coverage not audited) | UNKNOWN/PARTIAL pending mobile audit | client-side app state/models | Flutter app structure present | `dentix_mobile/test/`, pubspec/project tree | PARTIAL: repository presence does not prove production readiness |

## Ownership rule

The table is a navigation aid. It does not override endpoint permissions, model definitions, route registration, or tests. When a module spans several routers/services, the runtime files remain the authoritative ownership boundary.
