# Dentix Truth Source Map

`Runtime/executable source` wins over prose when there is a conflict. `Canonical source` is where humans should start; it may point back to executable files instead of copying them.

| Topic | Current source(s) | Conflicts / stale claims found | Runtime / executable source | Canonical source | Action required |
|---|---|---|---|---|---|
| Architecture | `PROJECT_STANDARDS.md`, supporting architecture docs, code structure | Older architecture text under-described async sessions and DB-level isolation | `backend/main.py`, `backend/routers/`, `backend/services/`, `backend/crud/`, `backend/database.py` | `PROJECT_TRUTH.md`, ADRs | Keep supporting architecture concise and evidence-linked |
| Branch workflow | `WORKFLOW_RULES.md`, CI/CD workflows, Git refs | Older prose described a `develop` CI trigger and earlier closeout text treated platform protection as unverified | Git refs + `.github/workflows/ci.yml`, `.github/workflows/branch-governance.yml`, `.github/workflows/platform-branch-protection.yml`, `.github/workflows/cd.yml` | `WORKFLOW_RULES.md`, `docs/product/GIT_RELEASE_GOVERNANCE.md` | Permanent model is protected `staging` + `main`; do not weaken strict rules for cosmetic ancestry sync |
| Environments | Workflow environments, development compose, env keys | Historical docs copied provider-specific assumptions | `.github/workflows/*`, `docker-compose.dev.yml`, `Dockerfile.do`, env definitions | `docs/product/ENVIRONMENT_AND_DEPLOYMENT_TRUTH.md` | Do not copy secret values |
| Deployment | Historical memory/deployment docs | DigitalOcean, Caddy/self-hosted compose, manual `deploy.py`, and GHCR publication were previously described as current | `.github/workflows/cd.yml`, root `Dockerfile`, `scripts/deployment/startup.sh`, `frontend/vercel.json` | `docs/product/ENVIRONMENT_AND_DEPLOYMENT_TRUTH.md`, `docs/product/DEPLOYMENT_ARTIFACT_DISPOSITION.md` | Retired surfaces remain historical only; do not recreate a second production path without ownership proof |
| Dependencies | Old requirements files and pip instructions | Multiple Python dependency sources previously drifted | `pyproject.toml`, `uv.lock`, frozen `uv` consumers in CI/containers | executable dependency files + deployment disposition | `pyproject.toml` + `uv.lock` are canonical; retired requirements compatibility files are not current truth |
| API | `docs/API.md`, frontend API modules | Prose API docs can drift | `backend/main.py` router registration + `backend/routers/` + schemas | Runtime registration; capability inventory | Do not treat unverified prose endpoint lists as exhaustive |
| Database | README/docs, code | Provider names have changed over project history | `backend/database.py`, `backend/models/`, `backend/alembic/` | ADR 0003 + runtime | Production rule: PostgreSQL; development/test compatibility code may differ |
| Migrations/startup | Historical startup/deploy snippets | Multiple migration authorities previously existed | `scripts/deployment/startup.sh`, `backend/scripts/preflight_migrations.py`, Alembic revisions | deployment truth + executable startup | Production startup delegates migration/bootstrap exactly once to preflight; do not add a second migration runner |
| Auth | README/docs + auth code | Historical token/session descriptions were incomplete | `backend/routers/auth/`, `backend/routers/password_reset.py`, `backend/services/auth_service.py`, auth tests | Runtime auth code + security links in `PROJECT_TRUTH.md` | Logout/reset revocation, access expiry, refresh expiry/rotation/replay, lockout/rate limiting and 2FA are regression-tested; revalidate before changing session semantics |
| CSRF/session behavior | Historical security notes | Older documents focus on JWT but omit current CSRF boundary | `backend/main.py`, `backend/routers/auth/login.py` | `PROJECT_TRUTH.md` security links | Cookie-auth state changes use CSRF validation; Bearer requests are treated separately |
| RBAC | Supporting architecture docs, feature route guards | Old compact matrices are incomplete | `backend/core/permissions.py`, router dependencies, RBAC tests | `backend/core/permissions.py` | Never duplicate the full role matrix in general docs |
| Tenant isolation | Old architecture/README, memory | Old prose described ORM filtering as the whole strategy | `backend/middleware/tenant.py`, `backend/core/tenancy.py`, `backend/database.py`, `backend/core/tenant_scope.py`, RLS migrations | ADR 0004 + executable security tests | Verified strategy is layered application scoping + PostgreSQL RLS; `rls-concurrency.yml` tests pooled-session reuse and adversarial HTTP tests exercise cross-tenant ID substitution |
| File/PHI access | Historical upload notes | Filesystem path safety and patient visibility are separate boundaries | `backend/routers/upload.py`, `backend/services/file_service.py` | runtime + security tests | Preserve component-aware path containment and authenticated tenant/patient visibility checks |
| Finance rules | Finance plans/current-state docs | Implementation plans can be mistaken for truth | finance services/routers, financial truth tests, frontend finance hooks/pages | `docs/FINANCE_METRIC_CONTRACT.md` when consistent with runtime + capability inventory | Keep formulas/contracts evidence-backed |
| Frontend routing | README/module docs | Routes change faster than prose | `frontend/src/App.jsx` | Module registry | Link rather than duplicate a giant route tree elsewhere |
| Public frontend topology | Historical hosting notes | HF-bundled SPA can be mistaken for the public custom-domain surface | `frontend/vercel.json` plus externally verified Vercel bindings | `docs/product/ENVIRONMENT_AND_DEPLOYMENT_TRUTH.md` | Public SPA/assets are verified on Vercel; `/api/*` routes to Hugging Face; direct HF SPA is secondary |
| Staging deployment verification | Earlier CD checked health only | Phase 9 requires production-like staging behavior, not only health | `.github/workflows/cd.yml`, `frontend/e2e/staging-deployment-smoke.spec.ts`, `frontend/playwright.staging.config.ts` | `docs/product/ENVIRONMENT_AND_DEPLOYMENT_TRUTH.md` | Keep HF staging smoke after deployment and fail promotion on auth/patient/appointment/finance/file/RBAC/frontend failures |
| Production container runtime | Earlier CI validated the build and static Dockerfile contract but did not start the image | Phase 4/9 acceptance requires actual non-root startup/preflight/health/permission proof | root `Dockerfile`, `.github/workflows/ci.yml`, `backend/tests/test_production_container_hardening_contract.py` | executable CI + deployment truth | Keep runtime smoke inside required `Dentix CI`; do not regress to build-only validation |
| PWA/stale assets | Generic PWA claims | Old guidance did not prove cross-deployment stale-hash behavior | Vite/PWA config, preload-recovery code, `.github/workflows/stale-deployment-recovery.yml`, deployed-staging asset monitoring | Phase 5 verification doc + runtime | Permanent A→B gate reproduces stale asset failure/recovery; deployed staging smoke watches for asset/PWA failures |
| Background jobs | `WORKFLOW_RULES.md`, memory | Historical “Prefect over Celery” wording overstates current orchestration | `backend/main.py`, `backend/workers/`, `backend/tasks/` | This map + runtime | Current lifespan starts in-process domain-event and subscription workers by default; other worker integrations require separate verification |
| AI stack | AI docs + historical plans | Provider/model descriptions can become stale | `backend/ai/`, `backend/services/ai/`, AI routers/config | `docs/AI_AGENT_STACK.md` as supporting; runtime wins | Avoid copying current model/provider names unless verified from config |
| Backup | Deployment/memory/settings docs | Old operational status may be stale | `backend/services/backup_service.py`, Google Drive client, relevant routers/settings | Capability inventory | Verified implementation supports PostgreSQL full dump and tenant JSON export followed by Google Drive upload; scheduling status is separate |
| CI | README + current-state snapshots | README/history contained old coverage/event assumptions | `.github/workflows/ci.yml` plus dedicated workflows in `.github/workflows/` | Workflows themselves | Keep thresholds/events/tool versions in executable workflow files; canonical container validation includes build + runtime smoke |
| Testing | README, plans, release gate | Some named test suites in older docs no longer match filenames | `backend/tests/`, `backend/ci_tests/`, `frontend/src/**/*.test.*`, `frontend/e2e/` | `PROJECT_TRUTH.md` links | Use discovery and executable workflows, not copied counts, as long-lived truth |
| Design system | `frontend/DESIGN.md`, UI code | Guidance can diverge from component reality | `frontend/src/shared/ui/`, Tailwind/CSS/config | `frontend/DESIGN.md` + shared UI code | Shared primitives own implementation truth |
| Mobile | Mobile plans and Flutter tree | A project tree does not prove production readiness | `dentix_mobile/`, `.github/workflows/mobile-responsive.yml` for web-responsive acceptance | Module registry/capability inventory | Do not confuse the web responsive gate with native-mobile production readiness |
| Logging/observability | README, old Sentry references | README claimed Sentry while runtime comments say Sentry removed | `backend/core/logging.py`, `backend/middleware/error_logging.py`, `backend/main.py`, system log router | `PROJECT_TRUTH.md` | Current verified observability includes internal structured/error logging, trace IDs, process timing, system logs and Prometheus instrumentation |
| Historical secret finding | Older Phase 8 prose treated the historical high-entropy signing credential as unresolved | Production/staging signing keys were rotated separately; encryption key was preserved; exact historical fingerprints are dispositioned by the history scanner | `.github/workflows/history-secret-scan.yml`, current key-derivation code, platform runtime configuration | `docs/product/PRODUCTION_ARCHITECTURE_NORMALIZATION_STATUS.md` | Do not broaden allowlists or copy secret values; new findings must fail the scan |
| Release status | Older checkpoint docs described production promotion as blocked | Protected-branch promotion already occurred; acceptance re-audit identified and is closing stricter Phase 4/8/9 evidence gaps | release PR history, platform protection gate, CI/CD, live production verification | `docs/product/PRODUCTION_ARCHITECTURE_NORMALIZATION_STATUS.md` | Final status becomes complete only after the acceptance-closeout changes pass staging production-like smoke and are re-promoted/verified on production |

## Precedence rule

A document is not made canonical merely by being newer. Current executable source, executable verification, and current platform evidence outrank prose. If two executable sources disagree materially, mark the topic `BLOCKED` until runtime ownership is established.
