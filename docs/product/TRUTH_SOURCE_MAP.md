# Dentix Truth Source Map

`Runtime/executable source` wins over prose when there is a conflict. `Canonical source` is where humans should start; it may point back to executable files instead of copying them.

| Topic | Current source(s) | Conflicts / stale claims found | Runtime / executable source | Canonical source | Action required |
|---|---|---|---|---|---|
| Architecture | `PROJECT_STANDARDS.md`, old `docs/ARCHITECTURE.md`, code structure | Old architecture text under-described async sessions and DB-level isolation | `backend/main.py`, `backend/routers/`, `backend/services/`, `backend/crud/`, `backend/database.py` | `PROJECT_TRUTH.md`, ADRs | Keep supporting architecture concise and evidence-linked |
| Branch workflow | `WORKFLOW_RULES.md`, CI/CD workflows, Git refs | CI still names `develop`, but no active `develop` branch was found during this audit | Git refs + `.github/workflows/ci.yml`, `.github/workflows/cd.yml` | `WORKFLOW_RULES.md` + this map | Treat `develop` workflow trigger as a documented cleanup candidate; Plan 01 does not change workflows |
| Environments | Workflow environments, compose files, env keys | Historical docs copied provider-specific assumptions | `.github/workflows/*`, compose/config, env definitions | `ENVIRONMENT_AND_DEPLOYMENT_TRUTH.md` | Do not copy secret values |
| Deployment | Old memory/rules, deployment docs | DigitalOcean described as current; old `deploy.py` instructions treated as current | `.github/workflows/cd.yml`; `frontend/vercel.json` on `main` | `ENVIRONMENT_AND_DEPLOYMENT_TRUTH.md` | Historical DO claims labeled non-current; GitHub Actions CD is current repo-controlled deployment path |
| API | `docs/API.md`, frontend API modules | Prose API docs can drift | `backend/main.py` router registration + `backend/routers/` + schemas | Runtime registration; capability inventory | Do not treat unverified prose endpoint lists as exhaustive |
| Database | README/docs, code | Provider names have changed over project history | `backend/database.py`, `backend/models/`, `backend/alembic/` | ADR 0003 + runtime | Production rule: PostgreSQL; development/test compatibility code may differ |
| Auth | README/docs + auth code | Historical token/session descriptions may be stale | `backend/routers/auth/`, `frontend/src/auth/` | Runtime auth code + security links in `PROJECT_TRUTH.md` | Revalidate before changing auth behavior |
| CSRF/session behavior | Historical security notes | Older documents focus on JWT but omit current CSRF boundary | `backend/main.py`, `backend/routers/auth/login.py` | `PROJECT_TRUTH.md` security links | Cookie-auth state changes use CSRF validation; Bearer requests are treated separately |
| RBAC | `docs/ARCHITECTURE.md`, feature route guards | Old compact matrices are incomplete | `backend/core/permissions.py`, router dependencies, `frontend/src/App.jsx`, RBAC tests | `backend/core/permissions.py` | Never duplicate full role matrix in general docs |
| Tenant isolation | Old architecture/README, memory | Old prose described ORM filtering as the whole strategy | `backend/middleware/tenant.py`, `backend/core/tenancy.py`, `backend/database.py`, `backend/core/tenant_scope.py`, RLS migrations | ADR 0004 | Current verified strategy is layered ORM scoping + PostgreSQL RLS for registered tables |
| Finance rules | Finance plans/current-state docs | Implementation plans can be mistaken for truth | finance services/routers, `backend/tests/test_financial_truth.py`, frontend finance hooks/pages | `docs/FINANCE_METRIC_CONTRACT.md` when consistent with runtime + capability inventory | Keep formulas/contracts evidence-backed |
| Frontend routing | README/module docs | Routes change faster than prose | `frontend/src/App.jsx` | Module registry | Link rather than duplicate a giant route tree elsewhere |
| Background jobs | `WORKFLOW_RULES.md`, memory | Historical “Prefect over Celery” wording overstates current orchestration | `backend/main.py`, `backend/workers/`, `backend/tasks/` | This map + runtime | Current lifespan starts in-process domain-event and subscription workers by default; other worker integrations require separate verification |
| AI stack | AI docs + historical plans | Provider/model descriptions can become stale | `backend/ai/`, `backend/services/ai/`, AI routers/config | `docs/AI_AGENT_STACK.md` as supporting; runtime wins | Avoid copying current model/provider names unless verified from config |
| Backup | Deployment/memory/settings docs | Old operational status may be stale | `backend/services/backup_service.py`, Google Drive client, relevant routers/settings | Capability inventory | Verified implementation supports PostgreSQL full dump and tenant JSON export followed by Google Drive upload; scheduling status is separate |
| CI | README + current-state snapshots | README/history contained old coverage/event assumptions | `.github/workflows/ci.yml` | Workflow itself | Keep thresholds/events in workflow only |
| Testing | README, plans, release gate | Some named test suites in older docs no longer match filenames | `backend/tests/`, `frontend/src/**/*.test.*`, `frontend/e2e/` | `PROJECT_TRUTH.md` links | Use discovery, not copied counts, as the long-lived truth |
| Design system | `frontend/DESIGN.md`, UI code | Guidance can diverge from component reality | `frontend/src/shared/ui/`, Tailwind/CSS/config | `frontend/DESIGN.md` + shared UI code | Shared primitives own implementation truth |
| Mobile | Mobile plans and Flutter tree | A project tree does not prove production readiness | `dentix_mobile/` | Module registry/capability inventory | Status remains `PARTIAL` pending dedicated mobile audit |
| PWA | README/plans | Generic PWA claims can be too broad | `frontend/vite.config.js`, `frontend/src/components/InstallPrompt*` | Module registry/capability inventory | Verified install manifest/service-worker configuration; API caching is NetworkOnly |
| Logging/observability | README, old Sentry references | README claimed Sentry while runtime comments say Sentry removed | `backend/core/logging.py`, `backend/middleware/error_logging.py`, `backend/main.py`, system log router | `PROJECT_TRUTH.md` | Current verified observability includes internal structured/error logging, trace IDs, process timing, system logs and Prometheus instrumentation |

## Precedence rule

A document is not made canonical merely by being newer. A current executable source or a test that verifies behavior outranks a newer prose statement. If two executable sources disagree materially, mark the topic `BLOCKED` until runtime ownership is established.
